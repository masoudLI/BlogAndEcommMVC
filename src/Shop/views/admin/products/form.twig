{{ csrf_input() }}
{{ field('title', item.title, "Nom du produit") }}
{{ field('slug', item.slug, "Lien") }}
{{ field('price', item.price, "Prix", {type: 'number'}, {step: '0.01'}) }}
{% if item.image %}
<img src="{{ item.thumb }}" alt="">
{% endif %}
{{ field('image', null, "Image", {type: 'file'}) }}
{{ field('pdf', null, "PDF", {type: 'file'}) }}
{{ field('published', item.published, 'En ligne', {type: 'checkbox'}) }}
{{ field('description', item.description, "Description", {type: 'textarea'}) }}
{{ field('created_at', item.createdAt.format('d F Y H:i'), 'Date de création', {class: 'datepicker'}) }}