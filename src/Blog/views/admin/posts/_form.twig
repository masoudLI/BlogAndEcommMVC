{{csrf_input()}}
{{ field('name', item.name, 'Titre de l\'article') }}
{{ field('category_id', item.categoryId, 'Categorie de l\'article', {options: categories}) }}
{{ field('slug', item.slug, 'Slug de l\'article') }}
{% if item.image %}
<img src="{{ item.thumb }}" alt="">
{% endif %}
{{ field('image', item.slug, 'Image', {type: 'file'}) }}
{{ field('published', item.published, 'En ligne', {type: 'checkbox'}) }}
{{ field('content', item.content, 'Slug de l\'article', {type: 'textarea'}) }}
{{ field('created_at', item.createdAt.format('d F Y H:i'), 'Date de l\'article', {class: 'datepicker'}) }}
